; (function () {
    function resize() {
        $(document).ready(function () {
            $('.top-bg-image').height($(window).height());
        })
         
    }

    // resize();
    window.addEventListener('resize', () => {
        // resize();
    })

    

})()